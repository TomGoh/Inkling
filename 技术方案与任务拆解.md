# Markdown 编辑器 —— 技术方案 & 任务拆解（面向 Coding Agent）

> 配套文档：`Markdown编辑器需求文档.md`（产品需求）
> 本文档目的：把需求转化为 coding agent 可以逐步执行、可验收的开发任务
> 使用方式：**不要把全部任务一次性丢给 agent**，按阶段逐个丢，每个任务做完你验收一次再进入下一个

---

## 1. 技术选型（已锁定，不再让 agent 选择）

| 层 | 选型 | 理由 |
|---|---|---|
| 应用外壳 | **Tauri 2.x** | 包体小、性能好、Rust 后端更安全，适合本地文件读写类应用 |
| 前端框架 | **React 18 + TypeScript** | 生态成熟，Milkdown 官方对 React 支持最完善 |
| 编辑器内核 | **Milkdown**（基于 ProseMirror） | 开源、已实现所见即所得，覆盖大部分 Markdown 语法，避免自研内核 |
| 状态管理 | **Zustand** | 轻量，够用，比 Redux 简单 |
| 样式 | **Tailwind CSS** + CSS 变量（用于主题切换） | 方便做自定义主题系统 |
| 公式渲染 | **KaTeX**（Milkdown 官方插件 `@milkdown/plugin-math`） | 比 MathJax 快 |
| 图表 | **Mermaid.js**（自定义 Milkdown 插件或代码块渲染钩子） | 官方库，文档全 |
| 代码高亮 | **Shiki** | 效果好，Milkdown 有对应插件 |
| PDF/Word 导出 | 调用本地安装的 **Pandoc**（Rust 侧 `std::process::Command` 调用） | 避免自己写格式转换逻辑 |
| 文件系统 | Tauri 内置 `fs`/`dialog` API | 原生支持 |

**技术选型验证任务（在正式开发前，先让 agent 单独跑一次）：**
> 任务：搭建一个最小 Tauri + React + Milkdown 项目，验证 Milkdown 能否实现：标题、加粗、斜体、有序/无序列表、任务列表、表格、代码块（带语法高亮）、图片插入。跑通后截图确认效果，再继续后续任务。
> 如果 Milkdown 在某几项上体验很差（比如表格拖拽调整列宽），记录下来，决定是自己扩展插件还是接受妥协。

---

## 2. 项目结构约定

```
markdown-editor/
├── src-tauri/                # Rust 后端
│   ├── src/
│   │   ├── main.rs
│   │   ├── commands/         # 暴露给前端的 Tauri command（文件读写、导出等）
│   │   └── fs_utils.rs
│   └── tauri.conf.json
├── src/                       # 前端
│   ├── components/
│   │   ├── Editor/            # Milkdown 编辑器封装
│   │   ├── Sidebar/           # 文件树
│   │   ├── Outline/           # 大纲面板
│   │   ├── StatusBar/         # 字数统计等
│   │   └── Settings/          # 偏好设置面板
│   ├── store/                 # Zustand store（当前文件、工作区、设置）
│   ├── themes/                # 内置主题 CSS
│   ├── lib/                   # 工具函数（导出、图片处理等）
│   └── App.tsx
└── package.json
```

**约定 agent 遵守：**
- 前后端通信统一走 `src-tauri/src/commands/` 里定义的 command，不允许前端直接拼路径操作文件系统之外的逻辑
- 每个功能模块一个文件夹，不许把所有逻辑塞进 `App.tsx`
- 所有新增依赖需在任务说明里注明，不许静默引入未讨论过的库

---

## 3. 分阶段任务清单

> 每个任务给 agent 时，格式建议：「实现 X 功能，验收标准是 Y，不要涉及 Z」

### 阶段一：骨架与基础编辑（对应 PRD P0 前半部分）

- [ ] **任务1**：初始化 Tauri + React + TS 项目，跑通"打开一个空窗口"
  - 验收：`pnpm tauri dev` 能正常启动窗口
- [ ] **任务2**：集成 Milkdown，实现一个占满窗口的编辑区域，能输入纯文本并支持基础 Markdown 快捷语法（标题/加粗/斜体/列表/引用/分割线）
  - 验收：输入 `# 标题` 自动变成大号加粗标题样式；输入 `**加粗**` 光标离开后正确渲染
- [ ] **任务3**：实现"打开文件夹" + 文件树侧边栏，点击 `.md` 文件在编辑区加载内容
  - 验收：选择一个包含多个 md 文件的文件夹，树状展示，点击切换正确加载对应内容
- [ ] **任务4**：实现保存（Ctrl/Cmd+S）与自动保存（防抖 2 秒）
  - 验收：编辑后不手动保存，2 秒后文件内容已写入磁盘（可用外部编辑器验证）
- [ ] **任务5**：状态栏字数统计（字数/字符数/预计阅读时长）
  - 验收：实时随编辑更新

### 阶段二：核心语法补全（对应 PRD P0 后半部分）

- [ ] **任务6**：表格支持（插入、拖拽调整列宽、行列增删）
- [ ] **任务7**：代码块 + 语法高亮（Shiki），支持切换语言、显示行号
- [ ] **任务8**：图片插入（拖拽/粘贴），自动复制到工作区 `assets/` 文件夹并用相对路径引用
- [ ] **任务9**：链接（含内部锚点跳转到标题）

> 每完成 2-3 个任务，建议让 agent 写一份简短的"实现说明"，方便你复查逻辑是否合理，而不是全靠肉眼测 UI。

### 阶段三：进阶功能（对应 PRD P1）

- [ ] **任务10**：数学公式（KaTeX 行内/块级渲染）
- [ ] **任务11**：Mermaid 图表渲染
- [ ] **任务12**：大纲面板（解析标题生成目录树，点击跳转，编辑同步高亮）
- [ ] **任务13**：导出 PDF/HTML（先做 HTML，再接入 Pandoc 做 PDF）
- [ ] **任务14**：主题系统（至少 2 套内置主题 + 支持用户自定义 CSS 文件）
- [ ] **任务15**：明暗模式切换

### 阶段四：体验打磨（对应 PRD P2）

- [ ] **任务16**：专注模式 / 打字机模式
- [ ] **任务17**：脚注、`[TOC]`、YAML Front Matter 支持
- [ ] **任务18**：查找替换（含正则）
- [ ] **任务19**：快捷键自定义面板
- [ ] **任务20**：导出 Word（.docx，走 Pandoc）

---

## 4. 给 coding agent 的通用约束（建议放进每次任务的提示词里）

1. 每次只做一个任务，不要顺带"顺手"实现后面阶段的功能
2. 完成后用一两句话说明改动了哪些文件、新增了哪些依赖
3. 遇到"选型建议"里没写清楚的细节（如某个 UI 交互怎么做），先给出你的方案说明，不要直接替我做决定后不声明
4. 涉及文件系统读写的代码，必须做错误处理（文件不存在、无权限等），不能假设一定成功
5. 不引入未讨论过的第三方付费/云服务

---

## 5. 建议的验收方式

- 每个任务合并前，实际打开应用手动测一遍，而不是只看 agent 的"完成"陈述
- 阶段一、阶段二完成后，用真实的一篇长笔记（比如你之前写的技术笔记）测试实际编辑体验，越早发现体验问题成本越低
- 阶段三之后，可以开始每周跑一次"回归测试"：随手写点内容，检查所有已实现功能是否还正常
