name: Build Windows

on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install frontend deps
        run: pnpm install --frozen-lockfile

      - name: Build Tauri app
        run: pnpm tauri build

      - name: Prepare portable zip
        shell: pwsh
        run: |
          $version = (Get-Content src-tauri/tauri.conf.json | ConvertFrom-Json).version
          $portableDir = "src-tauri/target/release/portable-build"
          New-Item -ItemType Directory -Path $portableDir -Force | Out-Null
          Copy-Item src-tauri/target/release/Inkling.exe "$portableDir/Inkling.exe"
          # 便携模式标记文件：存在则启用绿色模式，数据写入同目录 data 文件夹
          Set-Content -Path "$portableDir/portable.txt" -Value "This marker file enables portable mode. Keep it next to Inkling.exe. App data will be stored in the 'data' folder beside the exe."
          Compress-Archive -Path "$portableDir/*" -DestinationPath "src-tauri/target/release/Inkling-${version}-portable.zip" -Force
          Write-Host "Portable zip: Inkling-${version}-portable.zip"

      - name: Upload installers
        uses: actions/upload-artifact@v4
        with:
          name: Inkling-windows-installers
          path: |
            src-tauri/target/release/bundle/nsis/*.exe
            src-tauri/target/release/bundle/msi/*.msi
            src-tauri/target/release/Inkling-*-portable.zip
          if-no-files-found: error

      - name: Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            src-tauri/target/release/bundle/nsis/*.exe
            src-tauri/target/release/bundle/msi/*.msi
            src-tauri/target/release/Inkling-*-portable.zip
          generate_release_notes: true
